# go-url-shortener

### lib
+ gorilla mux
+ go-redis v9
+ godotenv
+ base58-go
+ testify

### router
+ `POST` /create-short-url
+ `GET` /{shortUrl} example: /4QhQ62cZem1


### RUN
`docker-compose up -d`